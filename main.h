#ifndef
#define MAIN_H

#include <limits.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
void printf(const char * const format, ...);
int _putchar(char c);

#endif
